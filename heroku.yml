build:
  docker:
    indexer: Dockerfile

release:
  command:
    - ./deployment-tasks.sh
  image: indexer
run:
  web:
    command:
      - ./target/release/metaplex-indexer-rpc -p $PORT
    image: indexer
  worker:
    command: 
      - ./target/release/metaplex-indexer
    image: indexer